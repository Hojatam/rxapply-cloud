{
  "name": "cowork-proxy",
  "version": "0.1.0",
  "description": "Tiny Node HTTP server that bridges n8n -> Claude Code / Cowork. Used by the rxapply-test phase only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "health": "curl -s http://localhost:7777/health"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "private": true
}
